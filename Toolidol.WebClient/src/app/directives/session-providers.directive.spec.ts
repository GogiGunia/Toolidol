import { SessionProvidersDirective } from './session-providers.directive';

describe('SessionProvidersDirective', () => {
  it('should create an instance', () => {
    const directive = new SessionProvidersDirective();
    expect(directive).toBeTruthy();
  });
});
